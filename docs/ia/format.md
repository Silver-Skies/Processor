# Instruction encoding and formatting

### Dynamic parameters
The only required parameter for an instruction to be valid is the operation identifier and other parameters may be expected based on how the specific operation corrosponding to the operation code needs are.

### Operation Codes
Operation codes (opcodes) are 8 bit inte

### Registers
Registers are referenced with 8 bit register identifiers. 